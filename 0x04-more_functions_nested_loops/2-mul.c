#include "main.h"

/**
 *mul - calculate the multiples of two integers
 *@a: first integer argument
 *@b: second integer argument
 *Return: the multiple of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
